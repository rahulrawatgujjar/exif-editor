Place ExifTool here for Windows packaging.

Required filename:
- exiftool.exe

Build behavior:
- ExifEditor.spec automatically bundles tools/exiftool.exe when present.
- build_windows_exe.bat prints a warning if missing.

Download:
- Official ExifTool for Windows:
  https://exiftool.org/
